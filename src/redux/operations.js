import {
  addContactsReguests,
  addContactsSuccess,
  addContactsError,
  getContactsReguests,
  getContactsSuccess,
  getContactsError,
} from './actions';

// export const getContacts = () => async (dispatch) => {
//     dispatch(getContactsReguests())
//     try { const contacts = await getContacts() dispatch(getContactsSuccess());} catch (error) { dispatch(getContactsError())}
// };
